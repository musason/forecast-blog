<h1>{{data.name}}</h1>
<img src="https://image.tmdb.org/t/p/w185{{data.poster_path}}" alt="">

<div>
    
   
    {{#each seasons}}
      {{#if ( this.name 'Season')}}
        <h2>{{this.name}}</h2>
        {{/if}}
           {{/each}}
  </div>